PORT=3001
NEAR_NETWORK=testnet
NEAR_CONTRACT_ID=mixit.your-account.testnet
NEAR_RPC_URL=https://test.rpc.fastnear.com
CORS_ORIGIN=http://localhost:5173
# Public single-service hosting: serve the built frontend alongside /api.
SERVE_FRONTEND=false
# Use 1 only behind Render's trusted proxy; leave 0 for direct local access.
TRUST_PROXY_HOPS=0
